# Learn jq

ブラウザで動くインタラクティブな jq 学習アプリケーション

## 概要

このプロジェクトは、[jq](https://jqlang.github.io/jq/) をブラウザで学べる Web アプリです。JSON データに対して jq フィルターをリアルタイムで実行でき、結果がすぐに表示されます。

## 主な機能

- 🎯 **リアルタイム実行**: フィルターを入力するとすぐに結果が表示
- 📝 **JSON 入力**: JSON データを直接入力して試験
- 🔍 **jq フィルター実行**: 公式 jq と互換のフィルター実装
- 🎨 **シンプルな UI**: 入力と出力を並べて表示

## 技術スタック

- **React 19** - UI フレームワーク
- **TypeScript** - 型安全な JavaScript
- **Vite** - 高速ビルドツール
- **jq-web** - WebAssembly による jq 実装

## セットアップ

### 前提条件

- Node.js 18 以上
- npm または yarn

### インストール

```bash
npm install
```

### 開発サーバーの起動

```bash
npm run dev
```

ブラウザで `http://localhost:5173` にアクセスしてください。

### ビルド

```bash
npm run build
```

### プレビュー

```bash
npm run preview
```

## 使い方

1. **JSON を入力**: 左上の JSON 入力パネルに JSON データを入力
2. **jq フィルターを入力**: 右上のフィルター入力パネルに jq コマンドを入力
3. **結果を確認**: 右下に実行結果がリアルタイムで表示

### 例

#### 基本的なフィルター

```jq
.                    # JSONをそのまま出力
.name                # nameフィールドを取得
.[]                  # 配列の全要素を列挙
```

#### 複雑なフィルター

```jq
.users | map(.name)  # usersから名前だけを抽出
.items[] | select(.price > 100)  # 価格が100を超える要素
```

## プロジェクト構成

```
learn-jq/
├── src/
│   ├── App.tsx          # メインコンポーネント（jqエディタ）
│   ├── App.css          # スタイル
│   ├── main.tsx         # エントリーポイント
│   └── index.css        # グローバルスタイル
├── package.json         # 依存パッケージ
├── tsconfig.json        # TypeScript設定
└── vite.config.ts       # Vite設定
```

## 開発

### 型チェック

```bash
npm run lint
```

### 注意事項

- jq-web は WebAssembbly を使用しているため、最新のブラウザが必要です
- ネットワークリクエストはありません（すべてブラウザで処理）

## ライセンス

MIT

## 参考資料

- [jq Manual](https://jqlang.github.io/jq/manual/)
- [jq-web GitHub](https://github.com/jqlang/jq-web)
- [React Documentation](https://react.dev)
- [Vite Guide](https://vitejs.dev)
  globalIgnores(['dist']),
  {
  files: ['**/*.{ts,tsx}'],
  extends: [
  // Other configs...
  // Enable lint rules for React
  reactX.configs['recommended-typescript'],
  // Enable lint rules for React DOM
  reactDom.configs.recommended,
  ],
  languageOptions: {
  parserOptions: {
  project: ['./tsconfig.node.json', './tsconfig.app.json'],
  tsconfigRootDir: import.meta.dirname,
  },
  // other options...
  },
  },
  ])

```

```
